# Database Configuration
# Required for production deployment
DATABASE_URL=postgresql://username:password@hostname:port/database

# Node Environment
NODE_ENV=production

# Port (automatically set by Vercel, but can be overridden for local development)
PORT=5000